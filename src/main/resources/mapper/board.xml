<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.enjoytrip.model.mapper.BoardMapper">

    <resultMap type="board" id="BoardMap">
        <id column="post_id" property="postId"/>
        <result column="title" property="title"/>
        <result column="create_time" property="createTime"/>
        <result column="content" property="content"/>
        <result column="user_no" property="userNo"/>
        <result column="board_type" property="boardType"/>
        <result column="hits" property="hits"/>
    </resultMap>

    <select id="select" parameterType="int" resultMap="BoardMap" >
        select *
        from board
        where post_id = #{postId}
    </select>

    <select id="selectAll" resultMap="BoardMap" >
        select *
        from board
    </select>

    <select id="selectAllType" parameterType="string" resultMap="BoardMap" >
        select *
        from board
        where board_type = #{type}
    </select>

    <select id="selectAllTypeAndUser" parameterType="map" resultMap="BoardMap" >
        select *
        from board
        where board_type = #{type} and user_no = #{userNo}
    </select>

    <insert id="insert" parameterType="board">
        <selectKey order="AFTER" keyProperty="postId" resultType="int">
            select max(post_id)
            from board
        </selectKey>
        insert into board
        (title, content, user_no, board_type)
            values (#{title},#{content},#{userNo},#{boardType})
    </insert>

    <update id="update" parameterType="board">
        update board
            set title=#{title}, content=#{content}, user_no=#{userNo}, board_type = #{boardType}
        where post_id=#{postId}
    </update>

    <delete id="delete" parameterType="int" >
        delete
        from board
        where post_id = #{postId}
    </delete>

</mapper>